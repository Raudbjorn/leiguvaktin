# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Services
GET        /apartments/mbl          controllers.MblController.scrape
GET        /apartments/visir        controllers.VisirController.scrape
GET        /apartments/bland        controllers.BlandController.scrape
GET        /apartments/             controllers.RootController.scrape

# Map static resources from the /public folder to the /assets URL path


    